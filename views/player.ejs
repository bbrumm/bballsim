<%- include('header') -%>

<%- include('links') -%>
<body class="bodyStyle">
    <h1><%= playerDetails[0].first_name %> <%= playerDetails[0].last_name %></h1>
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-8">
                <p>Team: <%= playerDetails[0].team_name %></p>
                <p>Overall Rating: <%= playerDetails[0].rating_ovr %></p>
            </div>
        </div>
    </div>
    <h2>Player Stats</h2>
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-8">
                <table class="dataTable">
                    <thead>
                        <th>Season</th>
                        <th>Games Played</th>
                        <th>Points</th>
                        <th>PPG</th>
                        <th>Rebounds</th>
                        <th>RPG</th>
                        <th>Assists</th>
                        <th>APG</th>
                        <th>Steals</th>
                        <th>SPG</th>
                        <th>Blocks</th>
                        <th>BPG</th>
                    </thead>
                    <tbody>
                        <% for(var i=0; i < playerDetails.length; i++) { %>
                        <tr>
                            <td>Current</td>
                            <td><%= playerDetails[i].games_played %></td>
                            <td><%= playerDetails[i].points_scored %></td>
                            <td><%= playerDetails[i].ppg %></td>
                            <td><%= playerDetails[i].rebounds %></td>
                            <td><%= playerDetails[i].rpg %></td>
                            <td><%= playerDetails[i].assists %></td>
                            <td><%= playerDetails[i].apg %></td>
                            <td><%= playerDetails[i].steals %></td>
                            <td><%= playerDetails[i].spg %></td>
                            <td><%= playerDetails[i].blocks %></td>
                            <td><%= playerDetails[i].bpg %></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>